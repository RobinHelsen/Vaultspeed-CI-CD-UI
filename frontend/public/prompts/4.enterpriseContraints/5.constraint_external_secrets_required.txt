External secrets management required

Context:
Secrets must be stored and retrieved from secure external systems.

Hard Constraints:
- MUST NOT store secrets in code or pipeline definitions.
- MUST retrieve secrets securely.

Required Implementation:
- Integrate with secret managers (e.g., Vault, Key Vault).
- Fetch secrets dynamically.
- Avoid plaintext exposure.
