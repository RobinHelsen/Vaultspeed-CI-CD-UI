Different team owns database deployment.

Database changes are deployed by a separate team or DBA group.
The CI/CD pipeline must produce deployment-ready artifacts that can be handed off rather than deployed directly.
