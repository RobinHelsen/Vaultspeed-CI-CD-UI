External secrets are required.

Secrets must remain outside the CI/CD platform and be retrieved securely at runtime.
Do not store credentials, tokens, or connection secrets directly in repositories, pipeline code, or static pipeline variables.

Use an external secrets manager and reference-based secret injection where supported.
Apply least-privilege access and avoid exposing secrets in logs, artifacts, or pull request validation runs.
